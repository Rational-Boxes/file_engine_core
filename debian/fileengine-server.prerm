#!/bin/sh
set -e

case "$1" in
    remove|deconfigure)
        if [ -d /run/systemd/system ] && \
           systemctl is-active --quiet fileengine.service; then
            systemctl stop fileengine.service || true
        fi
    ;;
esac

#DEBHELPER#

exit 0
